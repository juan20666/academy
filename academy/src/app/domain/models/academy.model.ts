export interface AcademyNavItem {
  id: string;
  label: string;
  icon: string;
  active?: boolean;
}

export interface AcademyTab {
  id: string;
  label: string;
  icon: string;
  active?: boolean;
}

export interface QuickAccessItem {
  id: string;
  label: string;
  icon: string;
}
